import { CommitteeContent } from "@/components/committee/CommitteeContent";

export const metadata = {
  title: "Committee — Social & Badminton Club",
};

export default function CommitteePage() {
  return <CommitteeContent />;
}
